import { PATH_NAME } from "./pathName";
import { USER_ROLE } from "./userRoles";
import { EMPLOYEE_MENU_LABEL, EMPLOYEE_PATH_NAME } from "./employeeMenuLabel";
import { OperationMenuLabel, OPERATIONS_PATH_NAME } from "./operationMenuLabel";
import { DRAWER_MENU_LABEL } from "./drawerMenu";
import { REPORTS_MENU_LABEL, REPORTS_PATH_NAME } from "./reportsMenuLabel";

export {
  PATH_NAME,
  USER_ROLE,
  EMPLOYEE_MENU_LABEL,
  EMPLOYEE_PATH_NAME,
  OperationMenuLabel,
  OPERATIONS_PATH_NAME,
  DRAWER_MENU_LABEL,
  REPORTS_MENU_LABEL,
  REPORTS_PATH_NAME,
};
